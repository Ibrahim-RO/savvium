export interface CTAData {
  titulo: string;
  botonTexto: string;
  botonLink: string;
}

export interface VisionData {
  titulo: string;
  descripcion: string;
  imagen: {
    src: string;
    alt: string;
  };
}